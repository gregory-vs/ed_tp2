{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug TP",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/tp3.out",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb",
            "preLaunchTask": "Compilar TP (Makefile)"
        }
    ]
}
